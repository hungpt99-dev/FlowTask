# Project Rules

- FlowTask manages one project at a time.
- Each project corresponds to one repository.
- Projects store runs, tasks, state, and events locally in `.flowtask/`.
- Do not commit `.flowtask/runs/` to version control.
- Do commit `.flowtask/rules/` to version control.
