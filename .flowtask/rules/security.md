# Security Rules

- Never print secrets, tokens, passwords, or keys.
- Never read `.env` files without approval.
- Never delete `.git` or project root.
- Never disable security checks.
- Never upload sensitive files.
- Commands are classified: safe (auto), risky (approval), dangerous (blocked).
