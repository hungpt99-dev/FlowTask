# Workflow Rules

- Tasks within a run execute sequentially by default.
- A task can depend on other tasks.
- Dependencies must complete before a task starts.
- State is saved after every meaningful action.
- Events are written for every state transition.
